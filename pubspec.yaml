name: find_my_car
description: "FindMyCar - Trovo la mia auto."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_map: ^8.3.0
  geolocator: ^12.0.0
  image_picker: ^1.2.2
  latlong2: ^0.9.1
  path_provider: ^2.1.5
  share_plus: ^13.1.0
  shared_preferences: ^2.5.5
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - car.jpg
